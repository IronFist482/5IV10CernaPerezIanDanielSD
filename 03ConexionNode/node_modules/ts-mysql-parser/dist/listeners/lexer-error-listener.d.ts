import { ANTLRErrorListener } from 'antlr4ts';
import { LexerError } from './errors';
import { MySQLLexer } from '../grammar/MySQLLexer';
export declare class LexerErrorListener implements ANTLRErrorListener<null> {
    error?: LexerError;
    lexer: MySQLLexer;
    constructor(lexer: MySQLLexer);
    syntaxError<T>(_: any, offendingToken: T | undefined, line: number, character: number, message: string): void;
}
