/**
 * Remove quotes from a string. Useful during the parsing process
 * when we need to extract identifiers.
 *
 * e.g. '"1"' => '1'
 *
 * @param text
 */
export declare function unquote(text?: string): string;
